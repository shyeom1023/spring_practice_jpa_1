spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/spring_practice_jpa_1?useUnicode=true&characterEncoding=utf-8
    username: yshss1
    password: test1
    driver-class-name: org.mariadb.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    properties:
       hibernate:
#         show_sql: true
         '[format_sql]': true

logging.level:
  '[org.hibernate.SQL]': debug
  '[org.hibernate.type]': trace
    